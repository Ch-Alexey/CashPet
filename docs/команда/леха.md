# Леха · Каркас, ядро, сдача

Mac · Claude Pro. Ты начинаешь проект и держишь его целостным: от тебя зависят все остальные в первые три дня.

## Владеешь

- Каркас: 4 модуля (`core`, `content`, `data`, `app`), Gradle, version catalog.
- CI в GitHub Actions: сборка, тесты, APK как артефакт. Защита `main`, CODEOWNERS.
- **Контракты** — вместе с Виталиком: модели `core`, API движка, формат JSON контента, `UiState` каждого экрана.
- Модуль `core`: кошелёк, план и остаток, покупки и отказ при нехватке, «Хочу потом», копилка и срок, закрытие периода, очки роста и стадии, состояние кота, симулятор трёх сценариев.
- Тесты `core` — список в разделе «Тесты» [плана](../03-план.md).
- README, описание архитектуры и данных, загрузка на платформу 29.09.
- Экспертная проверка — организуешь после 29.09.

## Не трогаешь без владельца

`app/` (Саня), `data/` и код `content/` (Виталик), JSON-контент (Ярик).

## Сроки

| Когда | Что | Кого разблокирует |
| --- | --- | --- |
| 19.09 | Каркас в `main`, пустышка ставится на vivo, CI зелёный | Всех |
| 20.09 | Контракты согласованы и влиты | Виталика, Саню, Ярика |
| 23.09 | Движок периода и `./gradlew :core:simulate` | Ярика (баланс), Виталика («Ускорить») |
| 25.09 | Ядро целиком покрыто тестами | Интеграцию |
| 26–27.09 | Ревью, баги интеграции, README, архитектура | Сдачу |
| 29.09 | Тег `v0.1.0`, APK, загрузка до 23:59 | — |

## Помогаешь

- **Виталику** — API ядра, баги ядра, ревью его PR в `data` и `content`.
- **Сане** — Kotlin в целом. Его первый PR с экраном разбери подробно, дальше ревью делает Виталик.
- **Ярику** — объясняешь, что значат красные тесты баланса. Одобряешь его JSON, если Виталик занят.

## Тебе помогают

- **Виталик** — контракты, ревью твоих PR в `core`.
- **Ярик** — числа экономики и сценарии для симулятора.

## Ревью

- Твои PR в `core` смотрит Виталик.
- Ты смотришь: любые изменения контрактов, `core`, CI, Gradle.

## Первый день

1. Окружение по [04-окружение](../04-окружение.md).
2. Добавить всех в Collaborators репозитория.
3. Каркас и CI, защита `main`.
4. Созвон с Виталиком по контрактам.
