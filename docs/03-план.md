# CashPet · План разработки

2026-09-18 · Промежуточная сдача — **29.09, 23:59**. Функционал — [01-функционал](01-функционал.md), числа — [02-экономика](02-экономика.md), установка — [04-окружение](04-окружение.md).

## Команда и зоны

| Кто | Инструменты | Зона | Тесты |
| --- | --- | --- | --- |
| **Леха** | Claude Pro · Mac | Каркас проекта, контракты, ядро экономики (`core`), симулятор, CI, ревью | Ядро: бюджет, покупки, копилка, период, рост, сценарии |
| **Виталик** | Claude Pro · Windows | Хранение (`data`), загрузка и проверка контента (`content`), ViewModel, демо-режим и «Ускорить», раздел взрослого | Сохранение, загрузка контента, тесты-сторожа контента и баланса, демо |
| **Саня** | учится сам · Mac | Весь интерфейс на Compose: экраны, навигация, тема, анимация кота; вся графика — ИИ-эскизы, векторная обводка | По желанию — смоук-тест главного экрана |
| **Ярик** | DeepSeek · Windows | Контент (задания, магазин, цели, подработки, соседи, словарь, тексты онбординга), настройка баланса, помощь Сане с графикой, имена персонажей, таблица требований, презентация, карточка RuStore | Не пишет; его правки проверяют тесты-сторожа |

## Архитектура одним взглядом

```
app/      Android: Compose-экраны, навигация, ViewModel, анимации     ← Саня (экраны), Виталик (ViewModel)
data/     Android: Room, сохранение профиля и истории                  ← Виталик
content/  Kotlin: JSON-контент + загрузчик + проверки                  ← Ярик (JSON), Виталик (код)
core/     Kotlin без Android: модели, экономика, период, рост,         ← Леха
          состояние кота, симулятор сценариев
```

Зависимости только сверху вниз: `app → data, content, core`; `data → core`; `content → core`. `core` не знает об Android, поэтому его тесты идут за секунды.

**Стек:** Kotlin, Jetpack Compose (Material 3), Navigation Compose, ViewModel + StateFlow, корутины, Room (KSP), kotlinx.serialization, внедрение зависимостей вручную (один `AppContainer`, без Hilt — меньше магии для новичков), JUnit 5 в `core` и `content`, GitHub Actions. minSdk 26 (Android 8.0), compile/target — последний стабильный, который предложит Android Studio. Разрешений Android нет ни одного, даже интернета.

## Что можно делать параллельно, а что нет

**Блокирующее — пока не готово, код в этой части не пишется:**

| Что | Кто | Кого блокирует | Срок |
| --- | --- | --- | --- |
| Каркас: модули, Gradle, CI, защита `main` | Леха | Весь код всех | 19.09 |
| Контракты: модели `core`, API движка, формат JSON контента, `UiState` каждого экрана | Леха + Виталик | Код Виталика, Сани, JSON Ярика | 20.09 |
| Движок периода и симулятор | Леха | Настройку баланса Ярика, «Ускорить» Виталика | 23.09 |
| `data` + загрузка контента | Виталик | ViewModel | 24.09 |
| ViewModel | Виталик | Подключение экранов Сани к настоящим данным | 26.09 |
| Проба графики: один кот, 3 стадии, по частям | Саня | Анимацию кота и всю остальную графику | 21.09 |

**Свободно параллельно, без ожидания:**

- Установка окружения и обучение Kotlin/Compose — все, с первого дня.
- Черновые экраны в Figma — Саня, до каркаса.
- Тексты заданий, словарь, онбординг, реплики — Ярик, до формата JSON (в обычном документе).
- ИИ-эскизы котов — Саня; описания, референсы словами и список нужной графики — Ярик.
- После контрактов: ядро (Леха) ∥ `data` и `content` (Виталик) ∥ экраны на фейковых данных (Саня) ∥ контент в JSON (Ярик).

**Правило контрактов:** после 20.09 модели `core`, формат JSON и `UiState` меняются только через PR с ревью Лехи — остальные на них опираются.

## Этапы до 29.09

### Этап 0 · 18–20.09 · Подготовка

| Кто | Задачи |
| --- | --- |
| Все | Окружение по [04-окружение](04-окружение.md); запустить пустой проект на телефоне; пройти «Kotlin для C#» (см. ниже) |
| Леха | Каркас (4 модуля), CI, защита `main`, CODEOWNERS; вместе с Виталиком — контракты; `CLAUDE.md` уточнить под код |
| Виталик | Изучить Room и kotlinx.serialization; вместе с Лехой — формат JSON и `UiState` |
| Саня | Выбрать генератор картинок (до 19.09); ИИ-эскизы 3 котов × 3 стадии; курс Compose; Figma: Дом, План, Магазин, Копилка, Задание, Итог периода — на 360 dp |
| Ярик | Имена котов, соседей, мамы-кошки и мошенника; описания котов и список всей нужной графики для Сани; по одному заданию на тему — на самопроверку и сестре Сани |

**Контрольная точка A (20.09):** каркас в `main`, контракты согласованы, приложение-пустышка ставится на vivo.

### Этап 1 · 21–25.09 · Параллельная разработка

| Кто | Задачи |
| --- | --- |
| Леха | Движок: кошелёк, план и остаток, покупки и отказ при нехватке, копилка и срок, закрытие периода, очки роста и стадии, состояние кота. Симулятор трёх сценариев (`./gradlew :core:simulate`). Тесты на каждое правило |
| Виталик | `data`: Room, сохранение и восстановление всего профиля. `content`: загрузчик и тесты-сторожа контента. С 23.09 — тест-сторож баланса поверх симулятора. Начать ViewModel Дома и Плана |
| Саня | Тема, навигация, нижнее меню, верхняя панель. Все экраны из [функционала](01-функционал.md) на фейковых `UiState` с `@Preview`. Общая панель обратной связи. Кот по частям с анимацией (моргание, дыхание, хвост) и перекраской |
| Ярик | Контент в JSON: 8 заданий (по 2–3 на тему), магазин, 3 цели, подработка, словарь, онбординг. С 23.09 — настройка баланса через симулятор. Для Сани: поиск бесплатных ассетов, журнал лицензий, простые предметы магазина в векторе |

**Контрольная точка B (25.09):** ядро покрыто тестами, данные сохраняются, все экраны существуют на фейковых данных, контент проходит проверки.

### Этап 2 · 25–27.09 · Сборка целого

| Кто | Задачи |
| --- | --- |
| Виталик | Все ViewModel; демо-режим: демо-профиль, «Ускорить», сброс; раздел взрослого с барьером |
| Саня | Подключение экранов к ViewModel; анимации реакций и роста; доступность: 48 dp, 16 sp, значки вместе с цветом, отключение анимаций и звука |
| Леха | Ревью, баги интеграции, сборка APK в CI, README и описание архитектуры и данных |
| Ярик | Таблица требований со статусами, список вопросов заказчику, правки контента по итогам прогонов |

### Этап 3 · 28–29.09 · Проверка и сдача

- 28.09: все прогоняют сценарий Приложения А на vivo, баги — в Issues, чиним только блокирующее.
- 29.09, 18:00: заморозка. Тег `v0.1.0`, APK из CI.
- 29.09, до 23:59: загрузка на платформу (Леха).

## Что сдаём 29.09 (п. 7.1 ТЗ)

| Материал | Кто |
| --- | --- |
| Ссылка на публичный репозиторий GitHub | Леха |
| README: стек, запуск, перечень реализованных требований | Леха |
| Рабочий APK (подпись не нужна) | Леха (CI) |
| Описание архитектуры и структуры данных | Леха, Виталик |
| Не менее трёх ключевых экранов: создание питомца, главный, бюджет или накопления | Саня |
| Таблица требований «реализовано / в работе / не начато» и план до финала | Ярик |
| Список вопросов и ограничений для заказчика | Ярик (собирает из раздела 11 рамок) |

Презентация по п. 7.1 к промежуточной сдаче **не нужна** — она входит в финальную сдачу (п. 7.2). Ярик начинает её после 29.09.

## Тесты

Требование ТЗ (п. 3.4, ответы заказчика, критерий 8.2): логика бюджета, списаний, накоплений и прогресса покрыта автотестами, и расчёты совпадают с нашими описанными правилами. Поэтому каждое правило из [экономики](02-экономика.md) пишется вместе с тестом, а не в конце.

**Леха (`core`):**

- Баланс никогда не уходит в минус.
- Покупка при нехватке отклоняется с объяснением, чего не хватает.
- Сумма плана не превышает доступное, остаток считается верно.
- Перенос в «Хочу потом» не меняет баланс и факт.
- Снятие из копилки уменьшает сумму и срок ровно так, как показано до подтверждения.
- Заработок внутри периода не входит в план, остаток переходит в следующий период.
- Очки роста и стадии на трёх сценариях совпадают с таблицами [экономики](02-экономика.md).
- Стадия не понижается, очки не сгорают.

**Виталик (`data`, `content`, демо):**

- Профиль, баланс, покупки, копилка, цель, прогресс восстанавливаются после перезапуска.
- Весь контент загружается; у задания 2–3 варианта, у каждого объяснение и награда > 0; суммы кратны 5 и не выходят за 999; каждая тема покрыта.
- Тест-сторож баланса: неравенства раздела 19 и три сценария на 5 периодов.
- «Ускорить» даёт те же результаты, что ручной прогон тех же решений; сброс демо возвращает исходное состояние.

## Процесс в Git

- `main` защищена: только через PR, CI зелёный, одно одобрение.
- Ветка на задачу: `имя/что-делаем`, например `sanya/plan-screen`.
- CODEOWNERS: `core/` — Леха; `data/`, код `content/` — Виталик; `app/` — Саня и Виталик; JSON-контент — одобряет Леха или Виталик.
- CI на каждый PR: сборка, все тесты, проверка контента, APK как артефакт.
- Новичкам в Git удобнее GitHub Desktop.

## Как Ярик работает с контентом и балансом

- Меняет только файлы в `content/src/main/resources/content/` и таблицу баланса. Код не трогает.
- Каждая правка — отдельный PR. Если тесты-сторожа красные, в логе CI написано, какое правило нарушено; исправляет и пушит снова.
- Баланс: меняет числа в JSON → запускает `gradlew :core:simulate` (на Windows без `./`) → смотрит таблицу трёх сценариев. До появления симулятора — `python tools\econ_sim.py`.
- DeepSeek — для черновиков. Каждый текст сверяется с требованиями к языку (раздел 20 рамок): предложения до 12 слов, числа кратны 5, без стыда и восклицаний в неудачном исходе, термин — с пояснением.
- Все тексты игры ведёт он один — единый тон.

## Графика: требования

Схема: **ИИ-эскиз → ручная векторная обводка по частям → анимация в Compose.** Бесплатные ассеты — для значков и фонов, по ходу.

**Эскизы (Саня):**

- Сервис генерации фиксируем один, выбирает Саня до 19.09 (ChatGPT, Kandinsky, Шедеврум или другой). Перед началом — прочитать условия использования: разрешено ли использовать и распространять результат.
- Каждую генерацию записываем в `design/ai-log.md`: дата, сервис и модель, запрос, что получилось, ссылка на условия использования и дата их проверки. Это требование ТЗ для ИИ-графики.
- В запросах **не называем существующих персонажей и не загружаем чужие картинки как образец**: «как Том и Джерри» — только описание стиля словами (мультяшный, антропоморфный, прямоходящий), иначе получится производная от защищённого персонажа.
- Нужно: 3 кота с разным силуэтом (например, пушистый, гладкошёрстный, вислоухий) × 3 стадии (малыш, подросток, взрослый), вид анфас, нейтральная поза, плоский мультяшный стиль.

**Векторизация (Саня):**

- Figma или Inkscape, экспорт SVG → импорт в Android Studio как Vector Drawable.
- Кот делится на части-слои: тело, голова, глаза открытые и закрытые, рот (улыбка, нейтральный, грустный), уши, руки, ноги, хвост.
- Общее поле 512 × 512 для всех частей одного кота; точки вращения (основание хвоста, шея) записываем в `design/README.md`.
- Базовые части — в оттенках серого, окрас задаётся в коде: цвет + отдельный слой узора (полоски, пятна, однотонный).
- Имена: `cat_<кот>_s<стадия>_<часть>.svg`, например `cat_fluffy_s2_tail.svg`.
- Единая толщина контура и ограниченная палитра.
- Исходники — в `design/`, готовые векторы — в `app/src/main/res/drawable/`.

**Чем помогает Ярик:**

- **Описания котов словами** — внешность, характер, чем отличаются силуэты. Из них Саня делает запросы к генератору.
- **Список всей нужной графики** — таблица: кот, стадии, эмоции, предметы магазина, цели, соседи, мошенник, фоны, значки. С пометкой, что нужно к 29.09, а что к финалу. Иначе что-то забудется.
- **Бесплатные ассеты** — ищет значки и фоны с лицензией CC0 или Apache 2.0 и записывает каждый в `design/licenses.md`.
- **Журнал ИИ-генераций** — следит, чтобы `design/ai-log.md` был заполнен: это пойдёт в документацию.
- **Простые предметы в векторе** — корм, мячик, кепка, когтеточка: это простые формы, их можно рисовать параллельно с котами. Файлы — через PR, как и контент.
- **Проверка различимости** — все 9 вариантов кота на каждой стадии в одной таблице: различаются ли они не только цветом.

**Бесплатные ассеты:** только с явной лицензией (CC0, Apache 2.0, OFL). Кандидаты: значки Material Symbols (Apache 2.0), наборы Kenney (CC0), шрифт Nunito (OFL). Каждый ассет записываем в `design/licenses.md`: что, откуда, лицензия, ссылка.

**Проба (Саня, до 21.09):** один кот × 3 стадии, разрезан на части, в Compose моргает и машет хвостом. Не получилось — решаем, что менять, сразу, а не 28.09.

## Kotlin для C#-разработчиков

- Официальный тур по Kotlin (kotlinlang.org → Docs → Kotlin tour) — 2–3 часа, почти всё знакомо по C#.
- Курс «Android Basics with Compose» (developer.android.com) — первые 3 раздела для Сани и Виталика.
- Соответствия: `class`/`data class` ≈ `class`/`record`; `val` ≈ `readonly`; `String?` ≈ nullable; корутины и `suspend` ≈ `async`/`await`; `map`/`filter` ≈ LINQ; `StateFlow` ≈ наблюдаемое свойство.
- Claude у Лехи и Виталика читает `CLAUDE.md` в корне — там правила проекта, чтобы код от двух Claude был в одном стиле.

## После 29.09

Доработка разрешена 16–20.10 для прошедших в финал (топ-10 объявят 15.10). Питч — 23.10.

- Мини-игры-подработки, соседи, подарки-события, мошенники, гардероб, бесконечная прогрессия.
- Подписанный релизный APK, ключ — вне репозитория.
- Отчёт о проверке на vivo.
- Экспертная проверка — организует Леха (см. ниже).
- Документация из 12 пунктов п. 5 ТЗ.
- Презентация (Ярик) по шаблону: слайды 7–11 обязательны; шрифт Montserrat; палитра из раздела 10 рамок.
- Резервное видео до 3 минут (scrcpy), карточка RuStore.

## Проверка заданий

**Для себя — неформально, в отчёт не идёт:**

1. **Самопроверка командой.** Каждое задание проходят двое, не автор, удачным и неудачным путём. Сверяют с требованиями к языку (раздел 20 рамок).
2. **Сестра Сани.** Саня даёт ей поиграть и устно спрашивает: что было непонятно, где запуталась, что понравилось. Замечания — в чат, правки — Ярику. Без документов и протоколов.

Когда: первые три задания (по одному на тему) — до того, как писать остальные (раздел 20 рамок); весь сценарий на vivo — 28.09.

**В сдачу — только экспертная проверка.** Критерий 8.4 и финальная сдача требуют «результаты пользовательской или экспертной проверки»: кто проверял, какие задания проходил, что получилось, что исправили. Организует Леха после 29.09.
