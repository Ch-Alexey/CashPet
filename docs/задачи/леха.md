# Леха · задачи

Обновлено: 2026-09-25, после PR #32.

## ✅ Можно сейчас

1. **25.09 утром — статус от Сани.** Нет графики и экранов — включить план Б: навигация, Дом и План целиком делаем мы с Виталиком (у него это тоже в задачах, пункты 2–3), Саня берёт графику кота и остальные экраны.
2. **Решить с Саней пакет экранов:** `app.ui` (контракты) или `ui` — Виталик ждёт ответа.
3. **К сдаче, 28.09:** README — перечень реализованных требований (взять из [09-требования](../09-требования.md)); раздел «Модули и поток действия» в [11-архитектура](../11-архитектура.md) — выжимка из контрактов; «Хранение» там уже готово.
4. **Напомнить Ярику** про слайды 7–11 (срок 28.09) и ручную проверку на телефоне.

## ⏳ Нельзя пока

| Задача | Ждёт | От кого |
| --- | --- | --- |
| Сценарий Приложения А на телефоне | Контент, `GameStore`, экраны | Ярик, Виталик, Саня |

## ✔ Сделано

- Ревью PR #25–#32 Виталика (ViewModel всех экранов, демо, раздел взрослого, база v2 с миграцией): правила соблюдены, контракты `UiState` приняты; две мелочи поправлены за Виталика (записаны у него). `./gradlew test assembleDebug` зелёный, 261 тест — 25.09.
- Симулятор `./gradlew :content:simulate` — таблицы трёх сценариев и неравенства по настоящим JSON, итог в `content/build/simulate.md`; совпадает с таблицами экономики до цифры. Общий `BalanceChecks` — одни проверки для симулятора и тест-сторожа; Python-модель помечена устаревшей — 25.09.
- Короткие `@SerialName` у `TxSource` — сохранение больше не зависит от имени пакета, тест-сторож на это — 25.09.

- Тест-сторож магазина по правилу ТЗ («от 8 позиций, по 4 каждого типа») — PR #15, конфликт с PR #13 Виталика разрешён.


- Движок недели — PR #10. Движок по контрактам v4: все правила раздела 4 с тестами; стратегии «разумная игра», «транжира», «скопидом» и `Autoplay` — движок совпадает с таблицами экономики неделя в неделю; тест «баланс и копилка не уходят в минус» на 300 случайных играх — 24.09.
- Модели `core` — PR #4.

- Документы: функционал, экономика v2, план, окружение, зоны, правки Ярику — `dc419ea`.
- Слияние ветки Виталика `vitaly/docs-fixes` — `678ea40`.
- Android SDK, переменные окружения, телефон по USB — 24.09.
- Каркас собран локально: `./gradlew test assembleDebug` зелёный, 9 тестов — 24.09, ждёт коммита.
- Каркас в `main`, CI зелёный — `ea55224`.
- Контракты v3, бот автоодобрения своей зоны — PR #2, одобрил бот, CI зелёный.
- Контракты v2, CODEOWNERS, правки по первому ревью — `3f72986`.
- Защита `main`: удаление и force push запрещены, PR с одобрением владельца кода, зелёный `build` — 24.09.
- CODEOWNERS с никами: Виталик `1Oblivious1`, Саня `Alexwww228` — 24.09.
- Пустышка установлена на Xiaomi по USB и запускается: холодный старт 2,1 с (по ТЗ ≤ 5 с) — 24.09.
