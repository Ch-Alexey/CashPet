# Леха · задачи

Обновлено: 2026-09-24.

## ✅ Можно сейчас

1. **Закоммитить движок через PR** — ветка `leha/engine`: движок недели, стратегии, автопрогон, 50+ тестов. После слияния Виталик начинает `GameStore`, ViewModel и «Ускорить».
2. **Симулятор `:content:simulate`** — печатает таблицы трёх сценариев по настоящим JSON (для Ярика). Ждёт `economy.json` от Ярика; до этого — на образцах Виталика.
3. **Напомнить Сане принять приглашение в репозиторий.**

## ⏳ Нельзя пока

| Задача | Ждёт | От кого |
| --- | --- | --- |
| Демо «Ускорить» в `app` | `GameStore` | Виталик |

## ✔ Сделано

- Движок недели по контрактам v4: все правила раздела 4 с тестами; стратегии «разумная игра», «транжира», «скопидом» и `Autoplay` — движок совпадает с таблицами экономики неделя в неделю; тест «баланс и копилка не уходят в минус» на 300 случайных играх — 24.09, ждёт PR.
- Модели `core` — PR #4.

- Документы: функционал, экономика v2, план, окружение, зоны, правки Ярику — `dc419ea`.
- Слияние ветки Виталика `vitaly/docs-fixes` — `678ea40`.
- Android SDK, переменные окружения, телефон по USB — 24.09.
- Каркас собран локально: `./gradlew test assembleDebug` зелёный, 9 тестов — 24.09, ждёт коммита.
- Каркас в `main`, CI зелёный — `ea55224`.
- Контракты v3, бот автоодобрения своей зоны — PR #2, одобрил бот, CI зелёный.
- Контракты v2, CODEOWNERS, правки по первому ревью — `3f72986`.
- Защита `main`: удаление и force push запрещены, PR с одобрением владельца кода, зелёный `build` — 24.09.
- CODEOWNERS с никами: Виталик `1Oblivious1`, Саня `Alexwww228` — 24.09.
- Пустышка установлена на Xiaomi по USB и запускается: холодный старт 2,1 с (по ТЗ ≤ 5 с) — 24.09.
