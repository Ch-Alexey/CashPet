# Леха · задачи

Обновлено: 2026-09-24.

## ✅ Можно сейчас

1. **Закоммитить через PR** (защита уже включена — прямой push в `main` не пройдёт): контракты v3 (второе ревью Виталика), правило «не меньше 10 в копилку» в симуляторе, файлы задач. Как — ниже, в «Первый PR».
2. **Созвон с Виталиком 25.09** — утвердить контракты v3; Виталик уже согласен, открытых вопросов нет.
3. **Напомнить Сане принять приглашение в репозиторий** — без этого он не сможет пушить, а CODEOWNERS для `app/` не заработает.

### Первый PR — команды

Своё одобрение GitHub не засчитывает, поэтому PR одобряет Виталик (второй владелец по CODEOWNERS). Если срочно — вы как админ можете слить в обход правил.

```bash
git switch -c leha/contracts-v3
git add -A
git commit -m "Контракты v3: второе ревью Виталика"
git push -u origin leha/contracts-v3
```

Дальше на github.com — «Compare & pull request» → «Create pull request». Когда CI зелёный и Виталик одобрил — «Merge», затем `git switch main && git pull`.

## ⏳ Нельзя пока

| Задача | Ждёт | От кого |
| --- | --- | --- |
| Модели и интерфейсы из контрактов — в код с `TODO()` | Созвон 25.09 | Леха + Виталик |
| Движок недели, симулятор `:core:simulate`, тесты | Каркас и контракты | Леха + Claude |

## ✔ Сделано

- Документы: функционал, экономика v2, план, окружение, зоны, правки Ярику — `dc419ea`.
- Слияние ветки Виталика `vitaly/docs-fixes` — `678ea40`.
- Android SDK, переменные окружения, телефон по USB — 24.09.
- Каркас собран локально: `./gradlew test assembleDebug` зелёный, 9 тестов — 24.09, ждёт коммита.
- Каркас в `main`, CI зелёный — `ea55224`.
- Контракты v2, CODEOWNERS, правки по первому ревью — `3f72986`.
- Защита `main`: удаление и force push запрещены, PR с одобрением владельца кода, зелёный `build` — 24.09.
- CODEOWNERS с никами: Виталик `1Oblivious1`, Саня `Alexwww228` — 24.09.
- Пустышка установлена на Xiaomi по USB и запускается: холодный старт 2,1 с (по ТЗ ≤ 5 с) — 24.09.
