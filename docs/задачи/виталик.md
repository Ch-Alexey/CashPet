# Виталик · задачи

Обновлено: 2026-09-24.

## ✅ Можно сейчас

1. **Контракты v4 утверждены** — все три твоих ревью приняты (раздел 10 [контрактов](../08-контракты.md)). Созвон 25.09 не нужен, если нет новых вопросов.
2. **Изучить Room и kotlinx.serialization** — хранение предлагаем одной JSON-строкой (раздел 6 контрактов), это упрощает `data`.
3. **Тесты-сторожа для текущего контента** — в `content/src/test`, рядом с `ContentFilesTest`: то, что не зависит от нового формата (цены кратны 5, у каждого товара цена > 0, id уникальны). Первый тест-сторож уже в каркасе. Ветка `vitaly/content-guards`.
4. **Окружение** на Windows по [инструкции](../04-окружение.md), Claude Code.

## ⏳ Нельзя пока

| Задача | Ждёт | От кого |
| --- | --- | --- |
| `GameStore` и ViewModel | Слияние PR `leha/engine` — движок готов, 24.09 | Леха |
| Демо-режим, «Ускорить», раздел взрослого | `GameStore`; `Autoplay.run(state, engine, ReasonableStrategy(...), Stage.ADULT)` уже есть | Ты |

## ✔ Сделано

- `data`: Room, `RoomGameRepository`, настройки, тест на сохранение прошлой версии — PR #5.
- Загрузчик контента по контрактам v4 с образцами всех файлов — PR #6.
- `TaskSession` — прохождение задания — PR #7.

- Экономика v2.1: округление, «Купить мечту», неравенство 8б; симулятор v3 — проверено, таблицы совпадают — `ca7c171`.
- Формат заданий (07), план и факт на экране «План», правки Ярику раздел 7 — `678ea40`.
- Работа через ветку `vitaly/docs-fixes` — правильный процесс.
- Три ревью контрактов: 20 замечаний, все приняты — контракты v4 утверждены (24.09).
