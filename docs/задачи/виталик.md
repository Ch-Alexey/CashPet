# Виталик · задачи

Обновлено: 2026-09-25, ночь.

## ✅ Можно сейчас

1. **Загрузчик `neighbors.json`** — имя соседа по `id` для строки соседа в задании и советов на Плане. Без него ViewModel задания и Плана не собрать. Онбординг и словарь загрузчик уже читает — PR #20 Лехи.
2. **ViewModel главных экранов** по `UiState` из `app/vm/UiState.kt`: Дом, План, Магазин, Копилка, Задания и прохождение задания (`TaskSession`), Итог недели. Сане они нужны в первую очередь.
3. **Демо-режим и «Ускорить»** — `GameStore.autoplay(ReasonableStrategy(...), Stage.ADULT)` уже есть и проверен тестом; осталось: вход из раздела взрослого, итог каждой недели на экране, «Сбросить демо».
4. **Раздел взрослого** — `AdultUiState` (черновик в `UiState.kt`), сброс и удаление профиля через `GameStore.delete`.
5. **ViewModel онбординга с созданием питомца** — по `content.onboarding` (PR #20): экраны идут списком с `kind`, черновик `OnboardingUiState` подогнать под него и развести имена с `content.OnboardingScreen`.
6. **План Б по экранам** — если утром 25.09 от Сани нет экранов: Дом и План вместе с их ViewModel.

## ⏳ Нельзя пока

| Задача | Ждёт | От кого |
| --- | --- | --- |
| Пакет для экранов: `app.ui` (контракты) или `ui` (задачи Сани) | Решение с Саней | Саня |

## ✔ Сделано

- Сверка 25.09: сторожа «фраза для каждой причины» и «неравенства раздела 19 на настоящей экономике», барьер сложнее (`a` от 23, без одинаковых цифр, `b` от 4), документы и контракты по коду.
- `UiState` всех экранов и примеры для `@Preview` (`PreviewSamples`); задачи Сани — брать их, а не объявлять свои — PR #18.
- `GameStore` — одно место, где игра меняется и сохраняется: сначала диск, потом экран, всё по очереди, «Ускорить» только в демо, восстановление после перезапуска — PR #17.
- Пластырь убран с витрины и из настоящего `shop.json` — PR #13.
- Правки движка по ревью: «Ускорить» посреди недели, «взять из копилки» только при покупке, `GoalAlreadyBought` — PR #12. Первое — настоящая ошибка в стратегии Лехи, хороший улов.
- `SettingsViewModel` и `AdultGateViewModel` с тестами — PR #11.
- `AppContainer` — ручной DI, всё ленивое, `app` не видит Room — PR #9.
- `TaskSession` — прохождение задания — PR #7.
- Загрузчик контента по контрактам v4 с образцами всех файлов — PR #6.
- `data`: Room, `RoomGameRepository`, настройки, тест на сохранение прошлой версии — PR #5.
- Три ревью контрактов: 20 замечаний, все приняты — контракты v4 утверждены (24.09).
- Формат заданий (07), план и факт на экране «План», правки Ярику раздел 7 — `678ea40`.
- Экономика v2.1: округление, «Купить мечту», неравенство 8б; симулятор v3 — проверено, таблицы совпадают — `ca7c171`.
