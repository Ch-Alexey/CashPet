# Виталик · задачи

Обновлено: 2026-09-24, 23:30.

## ✅ Можно сейчас

1. **`GameStore`** — один на приложение, в `AppContainer`: `StateFlow<GameState>`, `dispatch(action)` → `engine.apply` → `gameRepository.save` → новое состояние; `Result.Rejected` и `Feedback` — отдельным потоком событий для экранов (раздел 1 [контрактов](../08-контракты.md)).
2. **ViewModel главных экранов** по `UiState` из раздела 8: Дом, План, Магазин, Копилка, Задания, Итог недели. Сане они нужны в первую очередь.
3. **Демо-режим и «Ускорить»** — `Autoplay.run(state, engine, ReasonableStrategy(economy, catalog), Stage.ADULT)` уже в `core`, тест гарантирует Взрослого не больше чем за 4 недели.
4. **Барьер взрослого, мелочь:** сейчас выпадают 11 × 3, 12 × 4, 21 × 3 — их третьеклассник решит в уме. Предлагаю `a` от 23 и не из одинаковых цифр (22, 33…), `b` от 4 до 9.

## ⏳ Нельзя пока

| Задача | Ждёт | От кого |
| --- | --- | --- |
| Проверка всего на настоящем контенте | Слияние PR `leha/content-mvp` — контент готов, загрузчик его читает | Леха |

## ✔ Сделано

- `AppContainer` — ручной DI, всё ленивое, `app` не видит Room — PR #9.
- `SettingsViewModel` и `AdultGateViewModel` с тестами — PR #11, одобрил бот по новой общей зоне.

- `data`: Room, `RoomGameRepository`, настройки, тест на сохранение прошлой версии — PR #5.
- Загрузчик контента по контрактам v4 с образцами всех файлов — PR #6.
- `TaskSession` — прохождение задания — PR #7.

- Экономика v2.1: округление, «Купить мечту», неравенство 8б; симулятор v3 — проверено, таблицы совпадают — `ca7c171`.
- Формат заданий (07), план и факт на экране «План», правки Ярику раздел 7 — `678ea40`.
- Работа через ветку `vitaly/docs-fixes` — правильный процесс.
- Три ревью контрактов: 20 замечаний, все приняты — контракты v4 утверждены (24.09).
